const path = require('path');

module.exports = {
  config: path.resolve(__dirname, 'src', 'config', 'database.js'), // find file database.js
  'migrations-path': path.resolve(__dirname, 'src', 'database', 'migrations'), // folder create migrations
};
